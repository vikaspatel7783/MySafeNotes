<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

        <item android:id="@+id/menu_search_note"
            android:icon="@android:drawable/ic_menu_search"
            app:showAsAction="always"
            android:title="Search note"/>

        <item android:id="@+id/menu_add_note"
            android:icon="@android:drawable/ic_input_add"
            android:title="@string/appbar_add_note"/>

        <item android:id="@+id/menu_add_category"
            android:icon="@android:drawable/ic_menu_add"
            android:title="@string/appbar_add_category"/>

        <item android:id="@+id/menu_delete_category"
            android:icon="@android:drawable/ic_menu_delete"
            android:title="@string/appbar_delete_category"/>

</menu>